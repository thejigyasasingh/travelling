<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTPS everywhere, with one exception.

  Android blocks cleartext HTTP by default, and that default is right: it is
  what stops a hostile network silently downgrading a connection. So rather
  than setting `usesCleartextTraffic="true"` — which permits plaintext to
  *every* host, including production — cleartext is allowed only for the
  loopback addresses a developer's machine answers on.

  `10.0.2.2` is the host as seen from an Android emulator; `localhost` covers a
  device forwarded with `adb reverse`. Neither is reachable from a real network,
  so shipping this file is safe.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
